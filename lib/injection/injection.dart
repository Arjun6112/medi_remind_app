import 'package:get_it/get_it.dart';

final getIt = GetIt.instance;

void setupInjection() {
  // Register services, repositories, etc.
  // Example:
  // getIt.registerLazySingleton<MedicationRepository>(() => MedicationRepositoryImpl());
}
